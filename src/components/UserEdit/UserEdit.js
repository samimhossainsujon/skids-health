import React, { useEffect, useState } from 'react';
import { useLoaderData } from 'react-router-dom';

const UserEdit = () => {

    // const data = useLoaderData();
    // console.log('user eide',data)



    return (
        <div>
            user edit
        </div>
    );
};

export default UserEdit;